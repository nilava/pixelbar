// Copy to secrets.h and fill in. secrets.h is gitignored.
//
// Hardcoded credentials are the prototype answer, not the shipping one. The
// device has 24x8 pixels and no keyboard, so real provisioning means a SoftAP
// and a phone — that lands later. Until then this keeps the join out of the
// repository and out of the build log.
#pragma once

#define PIXELBAR_WIFI_SSID "your-network"
#define PIXELBAR_WIFI_PASS "your-password"
